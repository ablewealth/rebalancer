{
  "name": "enhanced-tax-harvesting-service",
  "version": "2.0.0",
  "description": "Enhanced Tax Harvesting Service with Neon Database Integration - Portfolio rebalancing and tax optimization platform",
  "main": "test-server.js",
  "scripts": {
    "test-server": "node test-server.js",
    "start": "node test-server.js",
    "debug": "node test-server.js",
    "build": "./build-netlify.sh",
    "build:react": "cd frontend && npm install && npm run build",
    "build:static": "echo 'Updating static distribution' && mkdir -p dist",
    "build:netlify": "./build-netlify.sh",
    "deploy:netlify": "./deploy-netlify.sh",
    "clean": "rm -rf frontend/build && rm -rf dist/*.html dist/*.js dist/*.css"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {},
  "keywords": [
    "tax-harvesting",
    "portfolio-optimization",
    "neon-database",
    "netlify",
    "rebalancer",
    "react",
    "typescript"
  ],
  "author": "Enhanced Tax Harvesting Service",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/ablewealth/rebalancer.git"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
